# Deployment-related files (add these to your .gitignore)

# Production environment file (contains secrets)
.env.production

# Docker Compose backend override (generated by deployment script)
docker-compose.backend.yml

# Deployment credentials
.deployment_credentials
